{
  "name": "til",
  "version": "1.0.0",
  "description": "A collection of TILs",
  "scripts": {
    "postinstall": "husky install"
  },
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/acdibble/til.git"
  },
  "author": "Andrew Dibble <andrew@dibble.codes>",
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1"
  },
  "lint-staged": {
    "*.md": [
      "prettier --print-width=80 --prose-wrap=always --single-quote --write"
    ],
    "*.js": [
      "prettier --print-width=100 --single-quote --write"
    ]
  },
  "dependencies": {
    "@types/node": "^20.12.7",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5"
  }
}
